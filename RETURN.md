# RETURN — キュウチャからピックへの返し方

こちら（GitHub）は**読み専用の掲示板**。キュウチャがここに書き込むことはできないので、
返しはオーナーが1回だけ手で運ぶ。**その1回を確実にするために、形式を固定する。**

## 画像を返すとき

- **その場でダウンロードできる形**で出す（オーナーが所定のフォルダへ入れて取り込む）
- 1回の返答に1枚。複数出すときは、どれが何かを本文で明記する
- 画像内の文字は **ID・名前・年齢だけ**。説明文・セリフ・吹き出しを焼き込まない

## 文章（設定案・意見・質問）を返すとき

**下のブロックをそのままの形で出す。** 散文でまとめない。
オーナーはこれをコピーしてテキストファイルにし、画像と同じフォルダに入れる。

```markdown
## RETURN
- 日付: YYYY-MM-DD
- 対象: 例) 06 沙耶 / 全体
- 種別: 設定案 / 画像 / 修正 / 質問 / 意見

### 出したもの
（何を出したか。画像なら何枚目が何か）

### 変えた点
（依頼のどれをどう反映したか。1行1件）

### 変えなかった点・できなかった点
（**ここを省略しない。** できなかったことを書いてくれるほうが助かる）

### こちらからの質問・提案
（無ければ「なし」）
```

## 参照画像が足りないとき

**推測で描かない。止まって、必要なファイルを名指しで要求してください。**
「新しい顔を作らない」が最優先なので、参照が無いまま進めるほうが違反になります。
ファイル名は [ASSETS.md](https://raw.githubusercontent.com/primeQUADRAFLOW/manga-bridge/main/ASSETS.md) にあります。

## やらないでほしいこと

- 設定を「確定」しない（確定はオーナー承認後にピックがファイルへ書く）
- 前のセッションの記憶で上書きしない。**現在の正は [SNAPSHOT.md](https://raw.githubusercontent.com/primeQUADRAFLOW/manga-bridge/main/SNAPSHOT.md)**
- 依頼していない要素を「ついでに」変えない（何が効いたのか分からなくなる）
